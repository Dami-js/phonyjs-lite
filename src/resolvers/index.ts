import PostResolver from './PostResolver';
import UserResolver from './UserResolver';

/* eslint-disable @typescript-eslint/no-explicit-any */
const resolvers: any = [UserResolver, PostResolver];

export default resolvers;
